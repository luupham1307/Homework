// Exercise 1: Write a function that takes an object and returns an array of its keys.
function takesOject(obj) {
    return Object.keys(obj);
}
// ex
var byMe = {
    myName: "Luu",
    myAge: "25",
}
var check = takesOject(byMe);
console.log(check)





