var temperature = 80;

var checkTemperature = (temperature > 80) ? "It is too hot"
    : (temperature > 70) ? "The temperature is just right"
        : "It is too cold";

console.log(checkTemperature)

