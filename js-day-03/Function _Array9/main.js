/**Viết một hàm nhận vào một chuỗi và trả về số nguyên âm mà nó chứa. */

function check(str) {
    const vowels = ['a', 'e', 'i', 'o', 'u'];
    let count = 0;

    for (let i = 0; i < str.length; i++) {
        if (vowels.includes(i) {
            count++;
        }
    }

    return count;
}



